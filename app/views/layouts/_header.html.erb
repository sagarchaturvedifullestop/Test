<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
	    <li class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span
	      <span class="icon-bar"></span>
        </li>
	  <div class="nav-collapse">
        <ul class="nav">
			<li class="<%=  activ_link("home") %>" ><%= link_to "Home", root_url %></li>
                        <li class="<%=  activ_link("about") %>" ><%= link_to "About eAppsCasts", about_path %></li>
			
                        <% if current_user %>
			<li><%= link_to_unless_current "Feedback", feedback_path %></li>
                        <li>
  			 Welcome <%= current_user.name %>! <%= link_to "Sign Out", signout_path %>
                        </li>
  			<% else %>
                         <li class="<%=  activ_link("login") %>" ><%= link_to "Signup or Login", login_path %></li>
                         
  			<% end %>
			<% if current_user %>
			<div class="navbar-search-login">
                         <%  else %>
                          <div class="navbar-search">
                          <%  end %>
				<%= form_tag episodes_path, :method => "get" do %>
				<%= text_field_tag :search, params[:search], :size => 35 %>
				<%= submit_tag "Search Episodes", :name => nil %>
				<% end %>
			</div>
	 	</ul>
	  </div>
    </div>
  </div>
</div>